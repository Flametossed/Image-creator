// preload.js
window.addEventListener('DOMContentLoaded', () => {
  // Nothing specific needed here for now, but this file is required by Electron
  // for security settings and contextBridge if needed in the future
})